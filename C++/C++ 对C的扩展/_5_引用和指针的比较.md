### 指针和引用

```c++
---》这个关于定义和初始化

1.指针有自己的内存空间,存储的是变量的地址，引用只是一个已存在变量的别名
2.sizeof 指针，在32为平台为4个字节，sizeof引用为原对象的大小
3.指针可以初始化NULL，而引用定义之初必须初始化，只能初始化为已有对象
4.指针需要解引用对原有数据操作，引用直接操作就可以
5.指针可以有多级，引用只有一级
6.指针和引用都可以配合`const`来达到不能修改原对象的数据


```
